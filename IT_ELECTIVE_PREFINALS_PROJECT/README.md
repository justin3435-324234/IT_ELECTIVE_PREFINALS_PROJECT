# Help Desk Management System

## Overview
ASP.NET Core 8.0 MVC Application built using Entity Framework Core with SQLite (`lycevm.db`).

## Setup and Execution
1. Verify `lycevm.db` exists in the project root directory.
2. Run `dotnet restore`
3. Run `dotnet run`